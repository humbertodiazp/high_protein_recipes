<%= tag.div class: 'flex text-xs text-gray-800', id: dom_id(recipe, :likes)   do  %>
        <%= recipe.likes_count %>
        <div class="flex">
            <%= button_to like_button_text(recipe),  recipe_like_path(recipe),  method: :patch  %> 
        </div>
<% end %>