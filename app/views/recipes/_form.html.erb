<%= form_with(model: recipe) do |form| %>
  <% if recipe.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
        <% recipe.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <h4>Ingredients</h4>
  <div>
    <%= form.fields_for :ingredients do |ingredient_form| %>
      <%= ingredient_form.hidden_field :id %>

      <div>
        <%= ingredient_form.label :description, style: "display: block" %>
        <%= ingredient_form.text_field :description %>
      </div>
    <% end %>
  </div>

  <div>
    <%= form.label :instructions, style: "display: block" %>
    <%= form.text_field :instructions %>
  </div>



  <div>
    <%= form.submit %>
  </div>
<% end %>
